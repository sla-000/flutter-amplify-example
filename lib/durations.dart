const Duration sDuration = Duration(milliseconds: 300);
const Duration mDuration = Duration(milliseconds: 500);
const Duration lDuration = Duration(milliseconds: 800);
const Duration xlDuration = Duration(milliseconds: 1200);
